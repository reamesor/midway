{
  "name": "midway",
  "version": "0.1.0",
  "private": true,
  "description": "THE MIDWAY — every cut comes home",
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "lint": "pnpm --filter web lint",
    "check:colors": "pnpm --filter web check:colors",
    "anchor:build": "bash programs/midway-treasury/build.sh",
    "anchor:test": "cd programs/midway-treasury && anchor test"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@10.33.2"
}
